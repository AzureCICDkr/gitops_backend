<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.UserDao">

    <resultMap id="userMap" type="com.example.demo.dto.User">
        <id property="userId" column="user_id"/>
        <result property="email" column="email"/>
        <result property="name" column="name"/>
        <result property="picture" column="picture"/>
        <result property="provider" column="provider"/>
        <result property="role" column="role"/>
        <result property="enabled" column="enabled"/>
    </resultMap>

    <select id="findByEmail" parameterType="string"  resultMap="userMap">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <insert id="insertUser" parameterType="com.example.demo.dto.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users
            (email, name, created_at,updated_at, provider, role, enabled)
        VALUES
            (#{email}, #{name}, now(), now(),  #{provider}, #{role}, #{enabled})
    </insert>

    <update id="updateUser" parameterType="com.example.demo.dto.User">
        UPDATE users SET
            name = #{name},
            role = #{role},
            enabled = #{enabled}
        WHERE email = #{email}
    </update>

</mapper>

